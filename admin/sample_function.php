<?php
function sum($k,$p)
{
 return $k+$p;
 only echo // will make a function with return
}
function sum($k,$p)
{
	$i=$k;
	$j=$p;
	echo $k+$p;
   // will make a function with no return
}
function sum($k,$p)
{
	$k=0;
	$p=10;
	
	return $k+$p;
  // will make a function with return
  // it is a non paramatric function as will always give the same value
}

// paramatric varibales $k,$p;
//&nbsp html code for space
//php array , implode, explode
?>